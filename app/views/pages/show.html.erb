<div class="container main-display">
	<%= button_to "Back to #{@adventure.title} Start", adventure_page_path(@adventure, @adventure.pages.find_by_name("start")), method: :get, class: "btn btn-warning" %>
	<h1><%= @page.name %></h1>

	<div>
		<%= linkify_page(@page).html_safe %>
	</div>
</div>
